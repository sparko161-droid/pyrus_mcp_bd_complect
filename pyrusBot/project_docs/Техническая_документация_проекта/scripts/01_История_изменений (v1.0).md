---
title: "01 История изменений — Скрипты"
audience: "internal"
pyrus_id: "MY4yVQQYZWL"
pyrus_parent: "HzQz5U8wZjh"
synced_at: "2026-08-10T12:01:02.000Z"
synced_hash: "sha256:097a67a90eb606327b389a3cfbf9904e"
---

- v1.0 (10.08.2026): Добавлен `pyrus_dump.ts` (`npm run pyrus:dump`) — выгрузка фактического состава формы или справочника из API Pyrus: поля с кодами, `id`, типами, вложенностью, вариантами выбора и `choice_id`, колонки справочника с примерами строк. Закрывает системный пробел: спецификация, собранная по коду бота, охватывает примерно треть формы и при этом выглядит полной.
- v1.0: Добавлен `lib/env.ts` — общая загрузка `.env`, клиент Pyrus, корни Базы знаний и страховка `requireConfirmation` для необратимых операций. Ни один скрипт больше не хардкодит ID разделов и не читает `.env` сам.
- v1.0: Добавлен `kb_dump.ts` — read-only выгрузка дерева Базы знаний (`--root`, `--json`). Заменяет удалённый одноразовый `scratch/dump_kb.ts`.
- v1.0: Удалён `sync_kb.ts` — мёртвая ранняя версия синхронизатора, вытесненная `kb_sync.ts` и нигде не использовавшаяся. Статья `LrqSKagEFNO` в Базе знаний осиротела, удаляется в Фазе 3.
- v1.0: Удалена папка `scratch/` (9 одноразовых скриптов). Среди них `wipe_structure.ts` и `wipe_children.ts` рекурсивно удаляли всё поддерево боевой Базы знаний без подтверждения. Восстановимы из коммита `619e2a2`.
- v1.0: `kb_sync.ts` — корень берётся из `PYRUS_KB_ROOT_PUBLIC`, а не из константы в коде; исправлены неявные `any` в `extractFrontmatter`.
- v1.0: `generate_agent_docs.ts` — обходит подпапки `scripts/` (подхватывает `lib/`) и удаляет зеркала исчезнувших скриптов, иначе в Базе знаний остаются статьи с удалённым кодом.
- v1.0: `generate_agent_docs.ts` — `PROJECT_DOCS_DIR` и заголовок корневого оглавления переведены на папку без префикса `00_`.
- v1.0: `kb_sync.ts` — из словаря `cleanTopicName` убрано частное правило для `00_Техническая_документация_проекта`; общее правило замены `_` на пробел даёт корректное «Техническая документация проекта».
- v1.0 (10.08.2026): Добавлен `.github/workflows/check.yml` — `npm ci` и `npm run check` на push в `master` и на каждый pull request. Локальные проверки обходятся флагом `--no-verify` и на чужом ПК могут быть не установлены; на стороне GitHub их обойти нельзя. Секреты воркфлоу не нужны: тесты работают с подменённой сетью, а линтер и карта связей — с файлами.
- v1.0 (10.08.2026): Руководства `PYRUS_SERVER_BOTS_GUIDE.md` и `PYRUS_FORM_SCRIPTS_GUIDE.md` перенесены из корня репозитория в раздел «Руководства по разработке» Базы знаний. В корне они не выгружались и не проверялись линтером.
- v1.0 (10.08.2026): Добавлен `bootstrap.ps1` — подготовка чистого ПК: проверка и установка Node.js, `npm install`, создание `.env` из примера, `npm run check` и сверка с Базой знаний. На PowerShell, а не на TypeScript, намеренно: скрипт обязан работать там, где Node.js ещё не установлен. Существующий `.env` не перезаписывается никогда, значения логина и ключа вписывает инженер лично. Файл сохранён в UTF-8 **с BOM**: Windows PowerShell 5.1 читает `.ps1` без BOM в системной кодировке и ломает разбор на кириллице.
- v1.0 (10.08.2026): `generate_agent_docs.ts` — роли агентов (`.agents/agents/`) выгружаются в Базу знаний в раздел «Роли агентов». До этого роли не выгружались вообще, хотя Фундаментальное правило 5 требует отражать в Базе знаний всю конфигурацию агентов. Исчезнувшая роль удаляется из зеркала, чтобы не выглядеть действующей. Заодно `initChangelog` проставляет `audience: internal` — без него новый раздел не проходил линтер.
- v1.0 (10.08.2026): Добавлен `integration_map.ts` (`npm run map`, `npm run map:check`) — карта связей форм и скриптов. Строится из фактов: коды полей из однозначных конструкций в исходниках, `form_id` и `catalog_ids` из frontmatter, состав полей из таблицы спецификации. Режим `--check` входит в `npm run check` и не даёт закоммитить карту, отставшую от кода: карта, которую никто не проверяет, протухает так же, как протухали Фундаментальные правила до линтера. Связывание по упоминанию названий отклонено: имена неоднозначны и не отвечают на вопрос «меняю поле, что сломается».
- v1.0 (10.08.2026): Добавлен `new_form.ts` (`npm run new:form`) — разворачивает канонический комплект статей формы из `templates/form/`: 6 статей пользовательской документации, 12 технических и оглавление формы со скриптами. Скаффолдер сам дописывает ссылку в оглавление родителя и запись в историю изменений клиента: Фундаментальные правила 1 и 2 перестают зависеть от памяти инженера. Номер формы берётся из уже занятых, перезапись существующей папки запрещена.
- v1.0 (10.08.2026): `kb_sync.ts` — версия `(vX.Y)` больше не попадает в заголовок статьи Базы знаний. Она остаётся в имени файла и в оглавлении раздела; в заголовке она превращала список статей в столбик одинаковых суффиксов. Соответствующее требование убрано и из правила версионирования.
- v1.0 (10.08.2026): `kb_sync.ts` — истории изменений во внутренних разделах получили имя раздела в названии («01 История изменений — Функции»). Раньше все назывались одинаково, и в поиске Pyrus их было не различить. Номер `01` сохранён: по нему статья остаётся первой в списке.
- v1.0 (10.08.2026): `kb_sync.ts` — корневой раздел описывается файлом `clients/_index.md`. Раньше его название и описание не вёл никто: опечатка «КЛЕНТЫ» держалась в Базе знаний, потому что править её было нечем.
- v1.0 (10.08.2026): `generate_agent_docs.ts` — из названий статей с правилами убрано расширение `.md` («Правило: pyrus-documentation-process» вместо «Правило: pyrus-documentation-process.md»).
- v1.0 (10.08.2026): Разовая уборка в Базе знаний по согласованию с владельцем: перенесён раздел «Скрипты» под «Банк решений», удалены пустые дубли разделов «Формы и скрипты» (`GvGdy0gal6k`) и «Скрипты/lib» (`GWilyQ7y0cU`), оставшиеся от ранней версии синхронизатора, и осиротевшие статьи «Скрипт: sync_kb.ts» (`LrqSKagEFNO`) и «Скрипт: kb_sync_state.json» (`JUyWDUqLtgt`), код которых удалён ещё в Фазе 0.
- v1.0 (10.08.2026): `kb_sync.ts` — корневые папки получили собственные узлы Базы знаний. Раньше `solutions_bank/_index.md` и `project_docs/_index.md` перезаписывали телом описание самого раздела 14, а их подпапки уезжали прямо под корень, хотя в Pyrus лежали под «Банк решений». Отсюда и потерянное описание раздела 14, и расхождение дерева.
- v1.0 (10.08.2026): `kb_sync.ts` — добавлены `MOVE?`/`MOVE`: узел, лежащий не под тем разделом, переносится только по флагу `--yes`. База знаний общая корпоративная, самовольная перестановка разделов видна всей организации. Родитель сверяется с фактическим положением в Pyrus, поэтому расхождение чинится само.
- v1.0 (10.08.2026): `kb_sync.ts` — недоступный раздел больше не синхронизируется внутрь: раньше при ошибке доступа дети всё равно уезжали под несуществующего родителя.
- v1.0 (10.08.2026): `kb_sync.ts` — маршрутизация деревьев по `PYRUS_KB_ROOT_PUBLIC` и `PYRUS_KB_ROOT_INTERNAL`. Клиентские кабинеты и внутренние разделы разводятся по корням; по решению владельца оба корня пока указывают на раздел 14, разграничение держится на правах доступа к узлу.
- v1.0 (10.08.2026): `doc_lint.ts` — документ без поля `audience` больше не проходит проверку: на этом поле держится маршрутизация между клиентским и внутренним деревом.
- v1.0 (10.08.2026): `generate_agent_docs.ts` — `synced_hash` снимается только у файлов, тело которых действительно изменилось. Раньше снимался у всех: каждая генерация гнала в Базу знаний полтора десятка неизменившихся статей и лишала раздел определения конфликтов — без хэша синхронизатор не отличает локальную правку от чужой правки в Pyrus.
- v1.0: `kb_sync_state.json` — 15 ключей перенесены на новый путь раздела; удалён мёртвый ключ `clients\00_Глобальные_Правила\UserDocs\01_Архитектура` (раздел `TwWuk5pEP3E` удалён в Базе знаний вместе с пустой локальной папкой).
- v1.0: Инициализация раздела
