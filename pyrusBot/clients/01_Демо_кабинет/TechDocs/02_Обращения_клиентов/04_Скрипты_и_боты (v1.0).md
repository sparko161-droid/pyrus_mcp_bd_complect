---
title: "14.01.2.4 Скрипты и боты"
audience: "tech"
pyrus_id: "IzwbUhGUTbw"
pyrus_parent: "IAvP9URcqnj"
synced_at: "2026-08-11T18:29:06.000Z"
synced_hash: "sha256:eb7f1e50e3435a9c1482ea74c4392ab1"
---

## Серверные боты

| Бот | Файл | Триггер | Что делает |
| --- | --- | --- | --- |
| TODO | `Forms/02_Обращения_клиентов/scripts/todo_bot (v1.0).ts` | комментарий / изменение поля / этап | TODO |

Для каждого бота указывается:

- **Какие поля читает** — по кодам.
- **Какие поля пишет** — по кодам. Это же попадает в карту связей.
- **Права бота:** участник формы с правами TODO; «Управляющий» нужен только для работы со справочниками.
- **Защита от зацикливания:** бот игнорирует комментарии с `author.id === request.user_id`, помнит `last_event_key`, ограничивает число повторов одного вопроса.

## Клиентские скрипты формы

| Скрипт | Триггер (`form.onChange`) | Что делает | Куда пишет |
| --- | --- | --- | --- |
| TODO | поля TODO | TODO | TODO |

Клиентский скрипт исполняется в браузере и **не заменяет** серверную проверку: пользователь может сохранить задачу через API мимо формы.

## Ограничения среды

- Таймаут: 60 секунд (1 секунда на бесплатном тарифе).
- Лимит запусков: 10 за 10 секунд на бота.
- Память: 256 МБ; запись на диск запрещена.
- Сторонние npm-пакеты недоступны — только `pyrus-api` и стандартная библиотека Node.js.
- Бот деплоится **одним файлом**: блоки из Банка функций (`src/lib/`) копируются в файл, а не импортируются.

## Что переиспользовано из Банка функций

| Блок | Зачем здесь |
| --- | --- |
| `FormModel` / `TaskModel` | рекурсивное чтение вложенных полей |
| `same_value` | не писать значение, которое не изменилось: каждая запись порождает комментарий и новый вызов хука |
| TODO | TODO |

## Логирование

Внутренние комментарии бота идут **без** `channel`, поэтому клиент во внешнем канале их не видит. Ошибки копятся за прогон и отправляются одним комментарием.
