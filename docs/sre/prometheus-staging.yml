global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - "/etc/prometheus/alerts.yaml"

scrape_configs:
  - job_name: "pyrus-mcp-staging"
    static_configs:
      - targets: ["pyrus-mcp-server:8000"]
